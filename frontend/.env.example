# URL do backend/API usado no build (tablet/app).
# Exemplo: http://168.190.90.2:5000
# Se vazio, o app usa /api (ideal quando você tem um proxy no mesmo host).
VITE_API_BASE_URL=

# Impressão direta via TCP/9100 (Desktop e APK) (ex.: elgin-i8-01.stik.local ou 192.168.0.50)
# Se VITE_PRINTER_HOST estiver vazio:
# - APK cai no fallback HTTP (/print)
# - Desktop usa o caminho do spooler/print_cli (quando disponível)
VITE_PRINTER_HOST=
VITE_PRINTER_PORT=9100
